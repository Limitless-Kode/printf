#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _putchar(char c);
int _putstring(char *str);
int _putint(int n);
int _printf(const char *format, ...);

#endif /* MAIN_H */
